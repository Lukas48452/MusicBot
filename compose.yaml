services:
  jmusicbot:
    build: .
    container_name: jmusicbot

    restart: unless-stopped

    volumes:
      - ./config:/app/config

    environment:
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx512m"
